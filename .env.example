# Generate with: openssl rand -base64 32
BETTER_AUTH_SECRET=
BETTER_AUTH_URL=http://localhost:4321

# SQLite file location (relative to the project root)
DATABASE_PATH=./data/waterstrip.db

# Uploaded images. Never inside public/ — that directory is build input.
UPLOAD_PATH=./data/uploads

# First admin account, created automatically by `npm run db:seed`
ADMIN_EMAIL=
ADMIN_PASSWORD=
ADMIN_NAME=Administrator
